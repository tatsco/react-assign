import React, { Component } from "react";

class Loading extends Component {
  render() {
    return (
      <button className="button is-large is-primary is-loading">Loading</button>
    );
  }
}

export default Loading;
